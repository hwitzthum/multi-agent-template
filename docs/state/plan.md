# Plan

<!-- Wird von der Initialisierung (docs/templates/initializer-prompt.md) gefüllt.
     Die vier Abschnitte sind Pflicht und werden von ./scripts/validate-ledger.sh
     geprüft. -->

## Aktuelle Strategie

Noch keine Strategie festgelegt. Die Initialisierung trägt hier die knappe
Gesamtstrategie des Projekts ein.

## Meilensteine

- Offen: Initialisierung des Projekts nach
  `docs/templates/initializer-prompt.md`; danach werden die ersten Tasks unter
  `docs/tasks/` frei.

## Offene Risiken

- Die zentrale Prüfung `scripts/verify.sh` prüft bis zur Initialisierung das
  Starterkit selbst (die Testsuite), nicht das Produkt. Die Initialisierung
  ersetzt sie durch die Produktprüfung.
- Ein erster echter Lauf verursacht Modellkosten; offene oder riskante Tasks
  starten ohne weitere Freigabe den Manager-Loop.

## Änderungsverlauf

- Noch keine Einträge.
